# 云端部署指南

## 方案：Render.com 免费部署（5分钟）

Render 提供免费的 Python Web 服务，部署后获得永久公网网址。

### 第一步：准备代码

已将部署文件打包在 `cloud_deploy/` 目录，包含：
- `app.py` —— Flask 后端（登录、提交、分析、生成Word报告）
- `login.html` / `index.html` —— 前端页面
- `analysis/analyzer.py` —— 批改引擎
- `test_data/answer_keys.json` —— 答案键
- `requirements.txt` —— Python 依赖

### 第二步：上传到 GitHub

1. 在 GitHub 创建新仓库（如 `physics-exam`）
2. 将 `cloud_deploy/` 目录内所有文件推送到仓库根目录

### 第三步：在 Render 部署

1. 访问 https://render.com 注册（可用 GitHub 直接登录）
2. 点击 "New +" → "Web Service"
3. 连接你的 GitHub 仓库
4. 配置：
   - **Name**: physics-exam
   - **Runtime**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python app.py`
   - **Plan**: Free
5. 点击 "Create Web Service"

### 第四步：获取网址

部署完成后，Render 会分配一个网址如 `https://physics-exam.onrender.com`

这就是你的永久公网网址，任何人打开即可登录、答题、提交，后台自动分析生成Word报告。

## 注意事项

- 免费套餐：每月 750 小时运行时间（足够全天候）
- 免费套餐：512MB RAM，足够使用
- 首次访问可能需等待 30 秒冷启动
- 数据存储在 Render 服务器上（建议定期下载备份）
