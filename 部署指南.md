# 云端部署指南

## 方案：PythonAnywhere 免费部署

### 当前系统
机械能守恒章节·四阶测试问卷系统（前测→动态纠错→后测→四份Word诊断报告）

### 部署文件清单
- `app.py` —— Flask 后端（登录、前测、动态纠错、后测、生成四份Word报告）
- `login.html` / `index.html` —— 前端页面（四阶问卷结构）
- `analysis/agent6_correction_generator.py` —— 第二大题动态生成器
- `analysis/agent2~5` + `report_utils.py` —— 四份诊断报告生成器
- `test_data/questions.json` —— 前测/后测题目（含标准答案）
- `test_data/correction_bank.json` —— 习题纠正库
- `requirements.txt` —— Python 依赖

### 更新步骤（已在 PythonAnywhere 部署过时）
1. 在 PythonAnywhere 的 Bash 终端运行：
```bash
cd ~/physics_exam
git pull
pip install --user flask python-docx
```
2. 回到 Web 标签，点击 Reload 按钮

### 全新部署步骤
1. 在 PythonAnywhere 创建 Beginner 账号
2. Bash 终端：`git clone https://github.com/Fanru-lab/physics-exam.git physics_exam`
3. 创建 Web 应用，WSGI 指向 `app.py` 的 `app`
4. `pip install --user flask python-docx`
5. Reload
